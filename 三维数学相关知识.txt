1.三维空间下，A物体想转到B物体的坐标系下，需要用B的世界矩阵的逆矩阵，乘以A的坐标。向量处理也是一样。
2.OpenGL的相机，初始在（0,0,0），头朝Y轴正，看向Z轴负。
3.4*4矩阵变成3*3矩阵后，对应的变换处理，就少了位移的部分。如果移动的是向量，也可以把w分量设置为0，也同样可以移除位移部分。
4.相机的lookAt矩阵，glm::lookAt(相机位置，看像的点，世界坐标系中的上方向)。